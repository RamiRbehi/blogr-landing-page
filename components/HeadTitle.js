import Head from "next/head"

const HeadTitle = () => {
  return (
    <Head>
        <title>Blogr</title>
        <meta name="description" content="Blogr Landing Page"/>
    </Head>
  )
}

export default HeadTitle